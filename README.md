# modal-vectorstore-builder

Basic ingest, embed, and vectorstore pipeline in built with Modal to autoscale to the amount of data.

Stores a vectorstore of the text locally in `vectorstore/`.

## Commands to run

`modal run ingest.py`
